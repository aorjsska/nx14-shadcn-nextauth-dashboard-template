import { Button } from "@/components/ui/button";

export default function Dashboard() {
    return (
        <div>
            Dashboard
            <Button>
                Theme Button
            </Button>
        </div>
    )
}